# all we have to do is to specify a noinst_ library here.
noinst_LIBRARIES = lib{{ cookiecutter.__feature_file_name }}.a
lib{{ cookiecutter.__feature_file_name }}_a_SOURCES = {{ cookiecutter.__feature_file_name }}.cpp

# oh wow, is this it? really?
